import React from 'react';
import PropTypes from 'prop-types';

class MasterPage extends React.PureComponent {
    constructor(props) {
        super(props);
    }

    render() {
        return (

        );
    }
}